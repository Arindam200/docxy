## What this changes

<!-- One or two sentences. What does it do, and why is it needed? -->

Closes #

## How I tested it

<!--
The commands you ran and what you saw. For a pipeline change, a run id and the
validation result it produced is the most useful evidence. Screenshots help for
dashboard changes.
-->

## Checklist

- [ ] `npm run lint`, `npm run typecheck`, and `npm test` pass from the repository root
- [ ] For `web/` changes: `npm run lint`, `npm run typecheck`, `npm run build`, and `npm test` pass in `web/`
- [ ] Docs updated, including `.env.example` if a new setting was added
- [ ] `CHANGELOG.md` updated under `## [Unreleased]` if a user would notice this
- [ ] No secrets, `.env` files, build output, or unrelated formatting changes
- [ ] One focused change, not several bundled together

## Notes for the reviewer

<!--
Anything you are unsure about, decisions you want a second opinion on, or
follow-up work you deliberately left out. Say so if this is a draft.
-->
